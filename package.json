{
  "name": "sigsum",
  "version": "0.1.1",
  "description": "Fast file hashing with BLAKE3. Native Rust speed for duplicate detection, integrity checks, and content addressing.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "native.cjs",
    "native.d.cts",
    "index.d.ts"
  ],
  "scripts": {
    "build:rs": "napi build --platform --release && mv index.js native.cjs",
    "build:ts": "tsup",
    "build": "pnpm build:rs && pnpm build:ts",
    "dev": "napi build --platform && mv index.js native.cjs && pnpm build:ts",
    "test": "vitest run",
    "bench": "vitest bench",
    "type-check": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format:rs": "cargo fmt --check",
    "clippy": "cargo clippy -- -D warnings",
    "release": "sh -c 'npm version $0 --no-git-tag-version && npx napi version && git add -A && git commit -m \"v$0\" && git tag \"v$0\" && git push origin main \"v$0\"'",
    "prepublishOnly": "napi prepublish -t npm",
    "version": "napi version"
  },
  "napi": {
    "binaryName": "sigsum",
    "targets": [
      "x86_64-apple-darwin",
      "aarch64-apple-darwin",
      "x86_64-unknown-linux-gnu",
      "x86_64-unknown-linux-musl",
      "aarch64-unknown-linux-gnu",
      "aarch64-unknown-linux-musl",
      "x86_64-pc-windows-msvc"
    ]
  },
  "keywords": [
    "blake3",
    "hash",
    "file-hash",
    "checksum",
    "duplicate-detection",
    "content-addressing",
    "integrity",
    "native",
    "napi"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/strazan/sigsum.git"
  },
  "author": "Sigge",
  "license": "MIT",
  "sideEffects": false,
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@napi-rs/cli": "^3.5.1",
    "@types/node": "^25.3.3",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
